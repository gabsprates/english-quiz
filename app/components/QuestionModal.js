const React = require('react');
const Requests = require('../services/Requests.js');

var QuestionModal = React.createClass({

  render: function () {
    return (
      <div>here the modal</div>
    );
  }

});

module.exports = QuestionModal;
